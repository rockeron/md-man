Screen {
  layout: vertical;
}

#body {
  height: 1fr;
}

#tree {
  width: 32;
  height: 1fr;
  border: round $surface;
}

#viewer-switcher {
  width: 1fr;
  height: 1fr;
}

#viewer {
  width: 1fr;
  height: 1fr;
  border: round $surface;
  padding: 1 2;
}

#markdown-view {
  width: 1fr;
  height: 1fr;
  border: round $surface;
  padding: 1 2;
  overflow-y: auto;
}

#status {
  height: auto;
  padding: 0 1;
  color: $text-muted;
}
